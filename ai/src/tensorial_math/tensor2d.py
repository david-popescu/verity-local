class Tensor2D:
    def __init__(self, rows: int, cols: int, data: [[int]]):
        self.rows = rows
        self.cols = cols
        self.data = data

    def populate(self):
        for i in range(self.rows):
            for j in range(self.cols):
                return self.rows
